# Python
__pycache__/
*.pyc
.venv/
venv/

# Build artifacts dell'eseguibile (PyInstaller)
build/*.spec
dist/
*.exe

# Config con credenziali (MySQL) - NON committare
config.ini

# Editor
.vscode/
.idea/

# NB: tcg_tracker.db e dashboard/data/*.json SONO committati apposta:
#     sono lo storico dei prezzi e i dati della dashboard.
# I backup del DB (creati prima delle migrazioni) NON vanno committati:
tcg_tracker.backup.db
*.backup.db

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# backup locali pre-migrazione DB (non committare)
*.premig.bak
*.predashboard.bak.db
*.bak
