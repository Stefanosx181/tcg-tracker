# Copia questo file in config.ini e compila se vuoi usare MySQL.
# Se ometti la sezione [mysql], il programma usa SQLite (tcg_tracker.db).
[mysql]
host = localhost
user = root
password =
database = tcg_tracker
